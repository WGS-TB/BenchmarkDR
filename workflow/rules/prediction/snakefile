#configfile: "config.yaml"
    
rule ml_run:
    input:
        "data/gene_data.csv",
        "data/AllLabels.csv",
    output:
        "ml/results.csv",
        "ml/lr.pkl",
    script:
        "scripts/ml_run.py"
